%html
  %head
    %title Weather

  %body
    This is a forecast: There will be weather.

    %form
      %label
        Address:
        %input{ name: 'address' }

    - if @location
      %section
        Current weather for
        = @location.zipcode

        Temparature
        = @weather.temperature.fahrenheit
